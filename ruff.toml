line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "W", "I", "UP"]

[lint.per-file-ignores]
# Package __init__ files re-export submodules for their side effects.
"__init__.py" = ["F401"]
